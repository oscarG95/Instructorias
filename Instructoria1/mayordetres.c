#include<stdio.h>
int main(){
    int a, b, c;
    scanf("%d", &a);
    scanf("%d", &b);
    scanf("%d", &c);
    procesar(a, b, c);
return 0;
}
void procesar(int x, int y, int z){
if(x>y){
if(x>z){
    printf("El mayor es %d", x);
}
else{ printf("El mayor es %d", z);
}
}
if
}
